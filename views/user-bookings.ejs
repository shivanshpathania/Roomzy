    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Your Bookings</title>
        <style>
            body {
                font-family: 'Segoe UI', sans-serif;
                background-color: #f3f6f9;
                margin: 0;
                padding: 0;
                color: #333;
            }

            .container {
                max-width: 1000px;
                margin: 50px auto;
                background: #fff;
                padding: 40px;
                border-radius: 20px;
                box-shadow: 0 12px 30px rgba(0, 0, 0, 0.1);
            }

            h1 {
                text-align: center;
                color: #0f3460;
                font-size: 2.5em;
                margin-bottom: 40px;
            }

            .booking-card {
                border: 1px solid #ddd;
                margin-bottom: 25px;
                padding: 25px;
                border-radius: 15px;
                background-color: #f9f9f9;
                box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
                transition: transform 0.3s ease, box-shadow 0.3s ease;
            }

            .booking-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            }

            .booking-card h3 {
                font-size: 1.8em;
                margin-bottom: 15px;
                color: #333;
            }

            .details {
                font-size: 1.2em;
                color: #555;
            }

            .details p {
                margin: 5px 0;
            }

            .no-bookings {
                text-align: center;
                font-size: 1.3em;
                color: #777;
            }

            .history-btn {
                display: block;
                margin-top: 40px;
                padding: 15px 30px;
                background-color: #0f3460;
                color: white;
                font-size: 1.3em;
                border-radius: 10px;
                text-align: center;
                text-decoration: none;
                width: 300px;
                margin: 0 auto;
                box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
                transition: background-color 0.3s ease;
            }

            .history-btn:hover {
                background-color: #1a4d8f;
            }

            .delete-btn {
            margin-top: 15px;
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1em;
            border-radius: 5px;
            cursor: pointer;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .delete-btn:hover {
            background-color: #c0392b;
        }

        </style>
    </head>
    <body>
        <div class="container">
            <h1>Your Bookings</h1>
            
            <% if (bookings.length === 0) { %>
                <div class="no-bookings">
                    <p>No bookings found. Go ahead and book a room!</p>
                </div>
            <% } else { %>
                <% bookings.forEach(booking => { %>
                    <div class="booking-card">
                        <h3><%= booking.hotelId.name %> - <%= booking.roomType %></h3>
                        <div class="details">
                            <p><strong>Price:</strong> ₹<%= booking.price %> / night</p>
                            <p><strong>Booking Date:</strong> <%= booking.createdAt.toDateString() %></p>
                        </div>

                        <form action="/api/bookings/<%= booking._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this booking?')">
                            <button type="submit" class="delete-btn">Delete Booking</button>
                        </form>

                    </div>
                <% }); %>
            <% } %>

            <a href="/hotels" class="history-btn">Explore More Hotels</a>
        </div>
    </body>
    </html>
